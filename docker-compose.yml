version: "3.9"

services:
  mongodb:
    image: mongo:7.0
    container_name: certshield-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  backend:
    build: ./backend
    container_name: certshield-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    depends_on:
      - mongodb
    volumes:
      - ./backend/keys:/app/keys

  # TODO (Sprint 4 — US-28): Uncomment and add frontend services
  # frontend-admin:
  #   build: ./frontend-admin
  #   container_name: certshield-admin
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     - backend

  # frontend-portal:
  #   build: ./frontend-portal
  #   container_name: certshield-portal
  #   ports:
  #     - "3001:3001"
  #   depends_on:
  #     - backend

volumes:
  mongo_data:
