# ZENCSE010
Universities, colleges, and training institutions issue digital certificates that can be easily duplicated or altered, leading to credential fraud. This project builds a certificate verification module — a Python Fast-Api microservice that:  Accepts certificate data via REST API Cryptographically signs it (ECDSA P-256) Generates a QR code.
